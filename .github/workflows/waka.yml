# name: Waka Readme

# on:
#   schedule:
#     # Jalan otomatis setiap jam 12 malam UTC (sekitar jam 7 pagi WIB)
#     - cron: '0 0 * * *'
#   workflow_dispatch: # Membolehkan kita men-trigger manual tombol "Run"

# jobs:
#   update-readme:
#     name: Update Readme with Metrics
#     runs-on: ubuntu-latest
#     steps:
#       - uses: anmol098/waka-readme-stats@master
#         with:
#           WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           # Opsi Tampilan
#           SHOW_OS: "False"
#           SHOW_PROJECTS: "False"
#           SHOW_EDITORS: "False"
#           SHOW_TIMEZONE: "False"
#           # Kita set "True" untuk Language karena ini yang Anda minta
#           SHOW_LANGUAGE: "True" 
#           SHOW_LANGUAGE_PER_REPO: "False"
#           SHOW_LOC_CHART: "False"
#           SHOW_DAYS_OF_WEEK: "False"

name: Work Stats Readme

on:
  workflow_dispatch:
  schedule:
    # Runs every 2 hours
    - cron: "0 */2 * * *"

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master 
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
